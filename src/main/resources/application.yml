spring:
  application:
    name: igmyeong-server

  datasource:
    url: jdbc:postgresql://localhost:5432/igmyeong
    username: postgres
    password: postgres

  jpa:
    hibernate:
    ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /docs